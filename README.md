Type `go get github.com/tjgillies/xchange`. This will download the program to your go bin folder.  
Then just type `xchange`. This will create a secret key file for you to diffie xchange with other people.  
Then type `xchange THEIR_PUBLIC_KEY` to create a shared key that you can use as a shared password.  